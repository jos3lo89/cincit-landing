<div class="dark-mode-gradient-bg"></div>

<style>
  .dark-mode-gradient-bg {
    /* 1. Posición fija para que cubra toda la pantalla */
    position: fixed;

    /* 2. Abarca desde los bordes (top, right, bottom, left) */
    inset: 0;

    /* 3. Lo envía detrás de todo el contenido */
    z-index: -10;

    /* 4. El degradado extraído del CSS original (era la variable --gradient-hero) */
    /* background: linear-gradient(
      135deg,
      hsl(220 25% 8%) 0%,
      hsl(220 30% 15%) 50%,
      hsl(220 25% 8%) 100%
    ); */

    /* background: linear-gradient(
      135deg,
      hsl(218, 88%, 10%) 0%,
      hsl(221, 53%, 9%) 50%,
      hsl(217, 45%, 10%) 100%
    ); */

    background: linear-gradient(
      135deg,
      hsl(218, 88%, 10%) 0%,
      hsl(221, 53%, 9%) 50%,
      hsl(221, 53%, 9%) 100%
    );

    /* Asegura que no cause un desbordamiento */
    overflow: hidden;
  }
</style>
